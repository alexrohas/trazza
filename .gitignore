preview*.png
.idea/
.DS_Store
Thumbs.db
node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.env.*.local

# Copia de trabajo del legado (repo propio dentro, no debe anidarse aqui)
tmp/
# Permisos de Claude Code especificos de cada maquina
.claude/settings.local.json

# Lienzos de diseno generados: llevan el editor embebido (~2,5 MB cada uno) y se
# regeneran desde los .dc.html y canvas.json de al lado. La regla va por patron y no
# por nombre: la primera version listaba solo hero-trazza.html y el de precios se colo.
design/**/*-trazza.html
